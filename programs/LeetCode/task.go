package main

import (
	"fmt"
	"math"
)

func main() {

	fmt.Scanln(&T)
}
func returnSum(list []int) (sum int) {
	//for _, num := range list {
	//	if !math.Signbit(num) {
	//		sum += num
	//	}
	//}

	if
	return
}
